{
  "name": "@perpetuals/contracts",
  "version": "0.0.2-dev.550",
  "private": true,
  "description": "perpetuals contracts",
  "scripts": {
    "build": "scarb build",
    "clean": "scarb clean && snforge clean-cache",
    "format": "pnpm sort-package-json && scarb fmt",
    "postinstall": "node ../../../../scripts/install-scarb.js",
    "lint": "scarb fmt --check",
    "test": "scarb test"
  }
}
